# 视频运动防抖算法研究 — PPT 汇报大纲

**目标时长**：5 分钟
**汇报对象**：授课教师及评委同学
**汇报材料参考**：本项目论文 `FinalPaper.pdf`、`data/results` 效果演示、`stabilizer/` 源码工程、流程图 `image/pipeline_flow.png`
**整体风格建议**：以核心原理图与直观效果对比（流程框图、轨迹图、并排对比视频）为主，文字精简。

> ⚠️ 算法已更新：现为 **Python + OpenCV 传统算法**（KLT 光流 + 4 自由度相似变换 + 高斯/L1 平滑 + 自适应裁剪/时域补边 + USM 锐化），**不再使用 VLFeat/SIFT，也不再做维纳去卷积去模糊**。讲稿请勿提及旧方案细节。

---

## 第 1 页：封面页 (Cover)
* **主标题**：基于光流跟踪与自适应运动补偿的视频防抖算法研究
* **副标题**：计算机视觉结课汇报
* **汇报人**：马子昂 及团队成员（蔡建宇、石兴霖、陆冲、黄俊源、马明义、刘琪瑞、聂方凯）
* **背景配图建议**：放置一张原始抖动视频帧与防抖后稳定帧的左右对比图作为背景。
* **🎤 演讲者备注**：
  > 各位老师、同学大家好。我们团队汇报的题目是《基于光流跟踪与自适应运动补偿的视频防抖算法研究》。本项目用纯传统计算机视觉方法（不依赖深度学习）解决便携设备拍摄视频时的高频随机抖动问题，并做到近实时运行。

## 第 2 页：研究背景与早期痛点 (Background & Motivation)
* **页面内容**：
  * **抖动的痛点**：高频震荡使主观体验变差，并对后续目标检测、跟踪、三维重建造成干扰。
  * **为何做传统方法**：深度学习在线防抖效果好但依赖大数据与高算力，难以在无 GPU 的嵌入式平台实时部署；传统几何方法轻量、可解释、易落地。
  * **前期算法踩坑**：项目初期自研「光流 + Harris 角点」，缺乏可靠性校验，遇到大面积移动前景（人群/车辆）会把前景运动误判为相机运动，快速摇摄时跟踪频繁丢失，输出反而更抖、还侵入黑边。
  * **破局思路**：基于 OpenCV 高效 C++ 后端重构三阶段流水线。
* **配图建议**：一张剧烈抖动 / 误判前景的示意。
* **🎤 演讲者备注**：
  > 视频抖动不仅影响观感，还会拖累后续视觉任务。我们最初自己写了光流加角点的方案，但它对动态前景特别敏感，常被走动的人群带偏。于是我们用 OpenCV 重构了一套更稳、更快的传统流水线。

## 第 3 页：三阶段模块化算法架构 (System Overview)
* **页面内容**：高度解耦的三阶段流水线
  1. **阶段一：鲁棒运动估计**（`motion_estimation.py`）
  2. **阶段二：参数分解与运动平滑**（`smoothing.py`）
  3. **阶段三：帧合成与画面增强**（`synthesis.py` / `border_fill.py`）
  * 配套自评指标模块 `metrics.py`（有效防抖时长占比）。
* **配图建议**：直接放论文流程图 **`image/pipeline_flow.png`**（输入 → KLT 光流+相似变换 → 参数分解+高斯/L1 平滑 → 自适应裁剪/时域补边+锐化 → 稳定视频）。
* **🎤 演讲者备注**：
  > 这是整体架构：运动估计、运动平滑、帧合成三大阶段，逐帧相对运动经平滑后得到矫正矩阵，再合成稳定画面。模块解耦让每一步可独立优化，也提升了运行效率。下面逐一介绍。

## 第 4 页：核心一 — 基于 KLT 光流的鲁棒运动估计
* **页面内容**：
  * **降采样 + Shi-Tomasi 角点 + 金字塔 KLT 光流**：用稀疏光流跟踪代替逐帧重检测匹配，相邻帧又快又稳；半分辨率（proc-scale 0.5）提速且对运动模糊更鲁棒。
  * **前向-后向一致性校验**：正向跟踪再反向跟回，回跳误差大的点判为不可靠并剔除（内点率提升到 0.95–1.00）。
  * **RANSAC 拟合 4 自由度相似变换**：稳健覆盖平移/旋转/缩放，剔除前景动态外点。
  * **回退机制**：估计失败时**沿用上一帧运动**（常速预测），而非置单位阵 —— 避免人为跳变。
* **配图建议**：KLT 跟踪连线图（背景点保留、前景点被校验/RANSAC 剔除）。
* **🎤 演讲者备注**：
  > 第一阶段追求「稳」。我们用 KLT 光流跟踪角点，再用前向-后向校验把不靠谱的点筛掉，最后用 RANSAC 拟合相似变换。注意一个细节：估计失败时我们沿用上一帧的运动而不是清零，这样轨迹不会突然跳一下。

## 第 5 页：核心二 — 参数分解与高斯 / L1 平滑
* **页面内容**：
  * **直接矩阵滤波的隐患**：直接对 3×3 矩阵插值平滑会破坏几何约束，产生剪切/畸变。
  * **本文解法**：矩阵连乘累积绝对轨迹 → 分解为 4 个独立参数（平移 x/y、旋转角、对数尺度）+ 角度解缠 → 在参数空间平滑。
  * **两种互补平滑器**：
    * **零相移高斯低通**：前后向双重滤波消除相位滞后（防抖不「慢半拍」），稳健、默认。
    * **L1 最优相机路径**（Grundmann）：把路径约束为分段静止/匀速/匀加速，线性规划求解，更利于保留运镜意图。
  * 矫正矩阵 `W = S · A⁻¹`。
* **配图建议**：轨迹图（红=估计原始、蓝=平滑、绿虚=真值）+ 高斯/L1 平滑示意。
* **🎤 演讲者备注**：
  > 拿到相机轨迹后，要滤掉高频抖动、保留低频运镜意图。我们不直接平滑矩阵（会扭曲画面），而是分解成平移、旋转、缩放四个参数再平滑。提供两种平滑器：高斯更稳，L1 更擅长保留有意的推拉摇移。

## 第 6 页：核心三 — 自适应裁剪 / 全幅时域补边 + 锐化
* **页面内容**：
  * **去黑边是帧合成的关键**（人眼对边缘伪影极敏感）。提供两条路径：
    * **预算约束自适应中心裁剪**：二分搜索每帧最小放大倍数 + 矫正量阻尼，全程小裁剪、贴近原画、**永不露黑边**。
    * **全幅时域运动补边**（Matsushita 思想）：用相邻帧真实像素 `G = A_j·A_i⁻¹·W_i` warp 过来填补黑边，**不裁剪、不损失视野与分辨率**，接缝羽化 + 残洞 inpaint 收尾。
  * **USM 锐化**：温和补偿三次插值的高频损失（强度 0.6，避免塑料感）。
* **配图建议**：左 = 裁剪 vs 补边对比；右 = warp 邻帧填补黑边示意。
* **🎤 演讲者备注**：
  > 防抖后画面边缘会出现黑边。我们给了两种方案：一种是自适应裁剪，自动算出刚好藏住黑边的最小放大，且永不露边；另一种更高级——用前后帧的真实像素把黑边补回去，完全不裁剪、不损失画面。最后做一点轻微锐化补偿插值损失。

## 第 7 页：实验评估 (Results & Analysis)
* **页面内容**：
  * **四个带真值的合成数据集**：手持步行、骑行颠簸、画面剧烈抖动、静止站立。
  * **两类指标**：
    * **运动估计精度（RMSE，估计轨迹 vs 真值）**：画面剧烈抖动 12.59 px（最准，无模糊）、手持步行 15.47 px、静止站立 18.83 px、骑行颠簸 48.66 px（最难，剧烈+模糊）。
    * **有效防抖时长占比（前 → 后）**：手持 0.6%→**57.6%**、剧烈抖动 1.2%→**56.4%**、静止 49%→**95.5%**；平均逐帧抖动由 7–9 px 降到约 1 px。
  * **平滑器对比**：高斯总体更稳（手持 57.6% vs L1 17.9%）；L1 在含主动运镜场景略优（剧烈抖动 59.4%、静止 96.1%）。
  * **效率**：运动估计约 3–7 ms/帧，335 帧整段处理仅数秒，无回退帧。
* **配图建议**（引用 `image/` 下重新生成的图）：
  * `synth_handheld_walking_trajectory_eval.png`、`synth_quick_panning_trajectory_eval.png`
  * （红=估计原始与绿=真值几乎重合 → 估计准；蓝=平滑后变平 → 抖动被去除）。
* **🎤 演讲者备注**：
  > 我们从两个角度评估：一是运动估计准不准——红线（估计）和绿虚线（真值）几乎重合，说明估得很准；二是防得稳不稳——有效防抖占比从个位数提升到 56% 以上，平均抖动从 7、8 个像素降到 1 个像素左右。高斯更稳健，L1 更会保留运镜意图。

## 第 8 页：演示与总结 (Demo & Conclusion)
* **页面内容**：
  * **视频 Demo 播放**：`data/results/*_compare.mp4` 左右并排（Original | Stabilized）。
  * **总结**：
    * KLT 光流 + 前向后向校验 + RANSAC 相似变换：高精度、抗动态外点、估计连续。
    * 参数分解 + 高斯/L1 平滑：无几何畸变、无滞后、可保运镜意图。
    * 自适应裁剪 / 全幅时域补边 + 锐化：彻底去黑边、贴近原画。
  * **能力边界与展望**：极端骑行颠簸超出 2D 相似变换建模范畴（仅部分改善）；后续可引入网格形变（Grid Warping）建模视差与卷帘快门（果冻效应）。
* **配图/视频建议**：嵌入 `data/results/*_compare.mp4` 作为压轴。
* **🎤 演讲者备注**：
  > 最后请看实机并排演示，左边原始、右边防抖后，画面明显更平稳。我们的方法在常见抖动上表现很好，极端骑行颠簸是当前 2D 模型的边界，未来可以用网格形变进一步处理。以上是我们的汇报，谢谢老师和同学！
