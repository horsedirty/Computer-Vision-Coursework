# 视频运动防抖与去模糊算法研究 - PPT汇报大纲

**目标时长**：5分钟  
**汇报对象**：授课教师及评委同学  
**汇报材料参考**：本项目论文 `FinalPaper.pdf`、`data/results` 效果演示、`vlfeat_stabilization` 源码工程  
**整体风格建议**：以核心原理图与直观的效果对比（轨迹图、视频）为主，文字精简。

---

## 第 1 页：封面页 (Cover)
* **主标题**：基于 VLFeat 与维纳去卷积的视频运动防抖算法研究
* **副标题**：计算机视觉结课汇报
* **汇报人**：马子昂 及团队成员（蔡建宇、石兴霖、陆冲、黄俊源、马明义、刘琪瑞、聂方凯）
* **背景配图建议**：可以放置一张原始存在重影的视频帧与防抖去模糊后的清晰帧的左右对比图作为背景。
* **🎤 演讲者备注**：
  > 各位老师、同学大家好。今天我们团队汇报的题目是《基于VLFeat与维纳去卷积的视频运动防抖算法研究》。本项目旨在解决便携设备拍摄视频时存在的高频随机抖动和运动模糊问题。

## 第 2 页：研究背景与早期痛点 (Background & Motivation)
* **页面内容**：
  * **视频抖动的痛点**：高频震荡导致主观体验极差，伴随的线性运动模糊（Motion Blur）给后续目标检测、追踪造成灾难性影响。
  * **前期算法踩坑**：项目初期使用“光流法 + Harris角点”，发现在遇到“大面积移动前景（如走过的人群）”时，算法会将前景运动误认为摄像机运动；并且缺乏尺度不变性，应对前后推进镜头时跟踪极易丢失。
  * **破局思路**：全面引入工业级算法库 **VLFeat**，结合高精度特征与物理去模糊模型。
* **配图建议**：
  * 一张展示剧烈抖动导致严重拖影模糊的照片。
* **🎤 演讲者备注**：
  > 在研究初期，我们自己编写了基于传统光流和Harris角点的防抖算法，但测试发现，当画面有大面积行人经过时，算法常常被“带偏”，甚至引起画面更严重的跳动。为了彻底解决动态外点的干扰和尺度变化问题，我们决定重构流水线，引入强大的VLFeat库。

## 第 3 页：三阶段模块化算法架构 (System Overview)
* **页面内容**：
  * 本文提出了一套高度解耦的三阶段流水线架构（基于 `run_vlfeat_stabilization.m`）：
    1. **阶段一：鲁棒运动估计** (代码: `vl_motion_estimation.m`)
    2. **阶段二：参数分解与运动平滑** (代码: `vl_motion_smoothing.m`)
    3. **阶段三：画面增强与非盲去卷积** (代码: `vl_frame_synthesis.m`, `vl_wiener_deblur.m`)
* **配图建议**：
  * 绘制一个从左到右的流程图：`视频输入` ➡️ `SIFT+RANSAC提取帧间矩阵` ➡️ `6自由度参数分解高斯平滑` ➡️ `物理运动推导模糊核+维纳滤波` ➡️ `稳定视频输出`。
* **🎤 演讲者备注**：
  > 这是我们重新设计的整体算法架构。我们将代码模块化为运动估计、运动平滑和画面增强三大阶段。模块化不仅使得每一步的优化互不干扰，也极大提高了程序运行的效率和可靠性。下面我们逐一介绍。

## 第 4 页：核心一：基于 VLFeat 的稳健运动估计
* **页面内容**：
  * **SIFT 特征提取**：利用 VLFeat 提取 128 维特征描述子。具备极强的尺度、旋转不变性以及部分光照不变性。
  * **RANSAC 鲁棒拟合外点剔除**：
    * 摒弃传统的最小二乘法（易被前景带偏）。
    * 随机抽取3对匹配点迭代，寻找内点（Inliers）支持数最多的 **6 自由度 (6-DoF) 仿射变换矩阵**。
* **配图建议**：
  * 插入一张图像匹配的连线图（可展示背景特征被保留，而动态前景点被 RANSAC 剔除）。
* **🎤 演讲者备注**：
  > 第一阶段的核心是稳。SIFT算法能帮我们找到绝对可靠的特征点，而 RANSAC 算法能像筛子一样，将那些属于运动行人和车辆的错误匹配点过滤掉。这样我们求得的 6 自由度摄像机运动矩阵就完全基于静态背景，极其精准。

## 第 5 页：核心二：参数分解与零相移滤波
* **页面内容**：
  * **直接矩阵滤波的隐患**：直接对 3x3 仿射矩阵进行插值平滑，会导致严重的画面几何畸变（剪切或非法缩放）。
  * **本文创新解法**：
    * **6参数拆解**：将绝对运动轨迹的矩阵分解为独立的 平移(x/y)、旋转、缩放 等6个物理参数。
    * **零相移高斯滤波**：采用 Zero-phase filtfilt 前后向双重滤波，避免了常规单向滤波造成的“相位滞后（画面防抖慢半拍）”现象。
* **配图建议**：
  * 图表：矩阵分解的数学公式示意，以及一个高斯平滑曲线示意图。
* **🎤 演讲者备注**：
  > 得到相机真实运动轨迹后，我们需要把其中高频的震动滤除，保留低频的运镜意图。我们不能直接对矩阵做均值滤波，那会造成画面被拉伸扭曲。我们把矩阵拆解为平移、旋转等独立参数进行高斯平滑，并且使用了零相移滤波，完美解决了画面滞后和变形的问题。

## 第 6 页：核心三：几何先验逆推物理模糊核与维纳滤波
* **页面内容**：
  * **常规盲反卷积痛点**：算力开销巨大（盲算模糊核）。
  * **直接“白嫖”运动参量（本文亮点）**：
    * 我们已经通过防抖求出了高精度的残差偏移量 $dx, dy$。
    * 直接从中提取模糊长度 $L$ 和角度 $\phi$，生成物理 **一维线性点扩散函数 (PSF)** (`vl_estimate_blur_kernel.m`)。
  * **非盲去模糊**：通过边缘渐变(Edgetaper)抑制水波纹，送入频域 **Wiener Filter** 维纳去卷积，算力极低且复原度极高。
* **配图建议**：
  * 左：残差向量 $(dx, dy)$ 推导运动模糊长度的示意。
  * 右：一张去模糊前的拖影图像 VS 维纳滤波后的锐利图像。
* **🎤 演讲者备注**：
  > 为了去除高频抖动带来的画面残影，我们设计了一个巧妙的策略：利用防抖计算出的位移残差，直接作为运动模糊的物理参数。这样免去了极度耗时的模糊核盲猜过程，配合频域的维纳去卷积，在几乎不增加耗时的前提下，使得画面清晰度获得了质的飞跃。

## 第 7 页：轨迹可视化与量化评估 (Results & Analysis)
* **页面内容**：
  * **三个评估数据集**：手持步行 (Handheld)、骑行颠簸 (Bumpy Riding)、画面剧烈抖动 (Severe Shake)。
  * **核心指标 (RMSE 均方根误差)**：
    * 剧烈抖动场景 RMSE 降至惊人的 **9.81 像素**。
    * 骑行颠簸与手持步行的误差也有效控制在合理区间内。
* **配图建议** (直接引用 `data/results` 文件夹下的绘图)：
  * 插入 `synth_handheld_walking_trajectory_eval.png`
  * 插入 `synth_bumpy_riding_trajectory_eval.png`
  * （说明：红线为原始抖动，蓝线为本算法平滑结果，绿虚线为Ground Truth）。
* **🎤 演讲者备注**：
  > 这是我们算法的量化表现。大家看图中的红线，它是原始视频那令人头晕的震动轨迹。经过我们的处理，蓝色实线变得极为平缓，且与代表理想真值的绿色虚线几乎完美贴合。在画面剧烈抖动场景下，我们实现了仅有 9.81 像素的极低误差。

## 第 8 页：演示与总结 (Demo & Conclusion)
* **页面内容**：
  * **视频 Demo 播放**：展示稳健、清晰的输出效果。
  * **总结**：
    * SIFT+RANSAC 筑基：高精度、抗动态外点。
    * 零相移参数分解平滑：无几何畸变、无滞后。
    * 物理先验去卷积：去模糊速度快、不盲目。
  * **未来展望**：后续考虑加入网格形变（Grid Warping）以彻底消除卷帘快门（Rolling Shutter/果冻效应）。
* **配图/视频建议** (利用 `data/results` 文件夹)：
  * 嵌入播放 `vlfeat_bumpy_stabilized.mp4` 或 `vlfeat_quick_panning_stabilized.mp4` 作为压轴。
* **🎤 演讲者备注**：
  > 最后请大家观看我们的防抖实机演示视频。可以看到不仅画面变得平稳，原有的残影也被显著消除。未来，我们还将探索网格形变技术来解决CMOS传感器的果冻效应。以上就是我们团队的结课汇报，感谢老师和同学们的聆听！
