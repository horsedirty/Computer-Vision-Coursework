# 视频运动防抖与画面去模糊：8人团队工作计划

> 基于 17 篇论文的系统性调研，采用「模块化三阶段流水线」架构（运动估计 → 运动分解与平滑 → 帧合成与去模糊），以 MATLAB + App Designer 为统一实现平台，兼顾 60% 抗抖动效果与 40% 算法效率的评分权重。

---

## 一、团队角色与分工（8人）

### 角色一：项目负责人 & 系统架构师（1人）

**核心职责：** 技术方案定稿、三阶段流水线的接口定义与模块间数据协议、算法效果的整体把控、进度管理与风险预警、结课报告统稿（引言 + 结论部分的执笔）。

**具体工作内容：**

- 研读全部 17 篇论文的总结文档，完成最终技术路线的决策——确定每个模块采用论文中的哪条技术路线、哪些做简化实现、哪些留作「未来工作」在报告中讨论
- 定义三阶段之间的数据接口：模块一输出运动场矩阵的格式（MATLAB struct 规范）、模块二输出平滑后的全局变换参数序列、模块三接收 warp 参数与帧图像并输出稳定帧
- 组织每阶段末的集成测试，确保三个模块的数据能正确流转
- 撰写结课报告中的摘要、引言（第 1 节）、结论与展望（第 5 节）、参考文献管理
- 制作答辩 PPT 的核心逻辑与框架

**需要精读的论文：** 14_LightStab（整体架构参考）、17_在线拼接防抖（相对坐标平滑创新点）、01_综述（方法定位）、00_技术选型（已完成的方案设计）。


### 角色二 & 角色三：运动估计工程师（2人）

**核心职责：** 负责三阶段流水线中的「模块一：运动估计」——从连续两帧中提取帧间运动信息，输出稠密运动场或全局仿射变换矩阵。

**具体分工：**

**角色二（关键点检测与匹配方向）：**
- 调用 MATLAB Computer Vision Toolbox 中的 `detectSIFTFeatures`、`detectSURFFeatures`、`detectORBFeatures`、`detectHarrisFeatures` 等函数，完成多检测器的独立调参与效果对比
- 实现关键点匹配：`matchFeatures` + `extractFeatures`，输出匹配点对集合
- 实现多检测器协作机制（参考论文 14_LightStab）：SIFT 与 SURF 双检测器取并集，加权融合后通过非极大值抑制合并候选点；在弱纹理区域检测失败时自动切换到 Harris 角点作为保底
- 实现关键点均匀化（参考论文 14_LightStab 的 Spatial Selective Clustering）：将画面划分为网格，每个网格内保留 Top-K 高置信度点，并对相邻点施加最小间距约束，避免纹理丰富区域的点过度聚集
- 统计各检测器在不同场景类型（静态背景、动态前景、弱纹理、大运动）下的匹配内点率，为后续自适应检测器切换提供数据依据

**角色三（光流与运动场融合方向）：**
- 调用 MATLAB Computer Vision Toolbox 中的 `opticalFlowFarneback`、`opticalFlowHS`、`opticalFlowLK`、`opticalFlowLKDoG` 等经典光流方法，对比精度与速度
- 针对实时在线需求，实现因果光流估计：每一帧只用当前帧与前一帧做前向光流计算，不访问未来帧
- 实现「稀疏关键点引导的光流融合」策略（参考论文 14_LightStab）：在关键点半径 r 的邻域内保留稠密光流的高精度，在邻域外用关键点对应关系做线性插值——抑制动态物体区域对稠密光流的污染
- 从融合后的运动场中使用 RANSAC 拟合单应性矩阵 `estimateGeometricTransform2D`，同时评估仿射变换（6 参数）与透视变换（8 参数）的拟合残差，选择残差更低的模型
- 输出模块一的最终产物：每帧对应的 3×3 全局变换矩阵（MATLAB affine2d 或 projective2d 对象），以及包含匹配点坐标、内点比率、拟合残差的辅助信息 struct

**两人协作节点：** 关键点检测器输出的匹配点集合是光流融合的输入；光流融合的精度反馈会影响关键点均匀化参数（邻域半径 r）的调整。每周需共同完成一次端到端测试：输入两帧 → 输出全局变换矩阵。

**需要精读的论文：** 14_LightStab（多检测器协作 + 稀疏光流融合）、10_光流防抖 CVPR2020（光流用于防抖的范式）、11_多网格 warping TIP（网格级运算参考）。


### 角色四 & 角色五：运动分解与平滑工程师（2人）

**核心职责：** 负责三阶段流水线中的「模块二：运动分解与平滑」——这是整个系统区分「相机抖动」与「物体真实运动」的核心模块，直接影响最终的去抖动效果。

**具体分工：**

**角色四（仿射分解与运动分离方向）：**
- 从模块一输出的每帧全局变换矩阵中，提取 6 个自由度的仿射参数序列：平移 (dx, dy)、旋转角 θ、缩放比 s、剪切分量 (shx, shy)
- 将 6 维参数序列绘制为时间曲线，观察并标注高频分量（=相机抖动，频率高幅值小）与低频分量（=有意运镜，频率低幅值大）的典型模式
- 设计自适应的运动分离策略：对仿射参数做短时傅里叶变换（`stft`），在频域设定自适应截止频率——低于截止频率的参数成分判为「有意运动」（保留），高于截止频率的判为「抖动」（需要平滑）。截止频率的选取需在多种运动模式下测试（步行手持、静止手持、快速扫视、慢速平移）
- 如果实现效果或时间有限，采用简化方案：直接对 6 维参数序列做低通滤波（IIR/Butterworth），截止频率作为可调参数，供后续实验调优
- 将分离后的全局运动残差（去除全局仿射变换后剩余的运动场）保存，作为「物体运动」分量的可视化依据——用于证明系统确实保留了目标主体的有效运动

**角色五（相对坐标马尔可夫平滑方向）：**
- 这是本方案最具学术区分度的技术点，参考论文 17_中北大学在线拼接防抖（MDPI Applied Sciences 2025）
- 实现相对坐标建模：不直接平滑绝对变换参数序列，而是将帧 t 的绝对变换参数 T_t 转换为帧间增量 ΔT_t = T_t - T_{t-1}，对增量序列做平滑，再从平滑后的增量序列重建绝对参数
- 实现窗口约束的马尔可夫平滑：平滑窗口半径 l=3（即每帧的平滑输出只依赖于当前帧及前后各 3 帧，共 7 帧窗口），窗口内用高斯核做加权平均，核的 σ 作为可调超参数。窗口前后重叠保证了长程平滑性，而有限窗口避免了全局累积误差
- 对比实验设计（这是 40% 效率分 + 60% 效果分的关键证据）：在同一段测试视频上，分别跑 3 种平滑策略的输出版本：
  - （a）纯高斯滤波（无相对坐标，无窗口约束）——baseline
  - （b）相对坐标高斯滤波（无马尔可夫窗口）
  - （c）相对坐标 + 马尔可夫窗口约束（完整版）
- 绘制原始轨迹 vs 平滑后轨迹的对比图：累积位移随时间变化的折线图——这是证明「保留了物体真实运动」最直观的可视化证据，也是结课报告中最有辨识度的图表
- 统计并对比三种平滑策略的运行时间（ms/帧），填入效率分析表

**两人协作节点：** 角色四负责从模块一拿到的全局变换中分解出 6 维参数序列，角色五在此基础上做相对坐标平滑。角色四的「运动分离」（区分抖动与运动）依赖角色五的平滑效果来验证：平滑后保留的低频成分应当对应角色的有意运镜，去掉的高频对应抖动。两人需共同确定 6 维参数序列的输出格式（MATLAB timetable 或数值矩阵），确保数据在两个子模块间流畅传递。

**需要精读的论文：** 17_在线拼接防抖（相对坐标马尔可夫平滑 + 动态注意力掩码）、09_GlobalFlowNet（仿射分解思路来源）、15_人工微跳视 SO(3)（递归优化热启动——如果平滑模块用了迭代优化，可以将上一帧的解作为当前帧初始值，加速收敛）、11_多网格 warping（全局 vs 局部运动的思想参考）。


### 角色六：帧合成与去模糊工程师（1人）

**核心职责：** 负责三阶段流水线中的「模块三：帧合成与去模糊」——将模块二平滑后的全局变换参数应用到原始帧，生成稳定帧，并去除残留模糊。

**具体工作内容：**

**帧合成（Warp）子模块：**
- 使用 MATLAB 的 `imwarp` 函数，以平滑后的仿射变换矩阵 `affine2d` 对原始帧进行几何变换
- 边界处理方案（参考论文 06_深度运动盲视频防抖 的全帧输出策略）：
  - 步骤 1：warp 后先对边界外区域做反射填充（`padarray` + `'symmetric'` 模式），避免黑边影响视觉效果
  - 步骤 2：轻微裁剪（`imcrop`），裁剪比例优先保持原始长宽比，裁剪量由最大平移幅值自动决定（计算整个序列中平移参数的最大绝对值，设置裁剪边距 = 1.2× 最大值）
  - 步骤 3：将裁剪后的帧缩放到统一输出分辨率（`imresize`）
- 对比测试 `affine2d` vs `projective2d` 的 warp 效果：在平移抖动主导的场景中仿射变换足够，在带有透视变化的场景中可能需要透视变换——统计两种变换下 warp 后图像的有效像素保留率，在结课报告中做定量比较

**去模糊子模块：**
- 首先评估是否需要去模糊：对 warp 后的帧计算拉普拉斯方差（`fspecial('laplacian')` + `imfilter` + `var`），清晰帧的拉普拉斯方差高、模糊帧的低。如果 warp 后帧的拉普拉斯方差相比原始帧提升超过阈值（如 15%），则跳过去模糊；否则将其送入去模糊模块
- 方案 A（优先实现，经典方法）：频域维纳去卷积（受论文 16_CompEvent 频域思路启发）
  - 对 warp 后的帧做 FFT（`fft2`），估计模糊核的点扩散函数 PSF（可以从运动参数中估计：模块二平滑掉的抖动分量的幅度和方向 = 模糊核的参数），用维纳滤波（`deconvwnr`）在频域做反卷积，再 IFFT 回到空间域
  - 模糊核的估计方法：从模块二平滑掉的「抖动分量」中，取该帧前后 ±2 帧的平均抖动方向作为模糊方向、抖动幅度作为模糊长度，生成线状运动模糊核
- 方案 B（进阶实现，深度学习方法）：使用 MATLAB Deep Learning Toolbox 导入轻量去模糊网络
  - 从 ONNX Model Zoo 或 GitHub 获取预训练的轻量去模糊网络（参考论文 12_门控时空注意力 CVPR2021 的简化版——两帧输入 + 通道注意力自适应融合），通过 `importONNXNetwork` 导入 MATLAB
  - 如果 ONNX 模型不可用，在 MATLAB 中手动搭建简化网络：一个 3 层卷积 + ReLU 的残差块（参考 `nnet.cnn.layer`），用论文 07_GoPro 数据集中的子集做微调训练（约 500 对模糊/清晰帧即可）
- 记录去模糊模块的运行时间（ms/帧），与 warp 时间合并为模块三的总延迟

**需要精读的论文：** 06_盲视频防抖（全帧输出 + 边界处理）、12_门控时空注意力（去模糊方法核心参考）、16_CompEvent（频域去模糊思路）、07_GoPro 手持去模糊（模糊核物理建模）。


### 角色七：MATLAB App Designer 前端工程师（1人）

**核心职责：** 基于 MATLAB App Designer 设计并实现完整的图形用户界面，将三阶段流水线的所有功能封装为可交互的桌面应用。

**具体工作内容：**

**界面布局设计：**

```
┌──────────────────────────────────────────────────────────┐
│  视频运动防抖与画面去模糊系统           — □ ×            │
├────────────────────┬─────────────────────────────────────┤
│                    │                                     │
│   [原始视频预览]   │   [处理后视频预览]                  │
│   UIAxes_Left      │   UIAxes_Right                      │
│                    │                                     │
│   ◄■■■■■■■■■■■■■■■■►│   ◄■■■■■■■■■■■■■■■■►              │
│   帧: 128 / 540    │   帧: 128 / 540                     │
│                    │                                     │
├────────────────────┴─────────────────────────────────────┤
│  控制面板                                               │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐   │
│  │ 加载视频  │ │ 开始处理  │ │ 暂停      │ │ 导出结果  │   │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘   │
│                                                         │
│  参数设置：                                              │
│  检测器: [SIFT ▼]  光流方法: [Farneback ▼]             │
│  平滑策略: [相对坐标马尔可夫 ▼]  窗口半径: [3]          │
│  去模糊: [频域维纳 ▼]  模糊核估计: [自动 ▼]            │
│                                                         │
│  ┌─ 实时指标 ──────────────────────────────────┐        │
│  │ 当前帧 PSNR: 32.4 dB  │ 平均帧率: 28.7 fps │        │
│  │ 当前帧 SSIM: 0.941    │ 处理延迟: 34.8 ms  │        │
│  └──────────────────────────────────────────────┘        │
│                                                         │
│  ████████████░░░░░░░░  进度: 67%  (362/540 帧)          │
└──────────────────────────────────────────────────────────┘
```

**核心功能实现：**

- **视频加载与预览：** 使用 `VideoReader` 读取测试视频，将每一帧显示在左侧 `UIAxes` 控件上。实现帧进度条（`UISlider`）支持拖动跳转到任意帧。支持显示当前帧号 / 总帧数。
- **处理后预览：** 将三阶段流水线输出的稳定帧实时更新在右侧 `UIAxes` 上。支持逐帧对比模式（左右同步跳帧）和分屏对比模式（左右两帧叠加在同一画布、半透明混合）。
- **参数面板：** 每个可调参数（检测器选择、光流方法、平滑策略、平滑窗口半径、去模糊开关、模糊核估计方式）对应一个 `UIDropDown` 或 `UISpinner` 或 `UISwitch`，参数改变时触发回调函数更新对应模块的 MATLAB 工作区变量。
- **处理控制按钮：**
  - 「加载视频」→ 弹出文件选择对话框 `uigetfile`，初始化 `VideoReader` 对象，获取总帧数、分辨率、帧率
  - 「开始处理」→ 调用三阶段流水线的主函数，启动 `parfor` 或手动分帧循环（注意 MATLAB 的并行计算工具箱在此处用于加速逐帧处理）
  - 「暂停」→ 设置全局标志位，在每帧处理循环中检查该标志位，为 `true` 时 `break`
  - 「导出结果」→ 使用 `VideoWriter` 将处理后帧序列编码为 `.mp4` 或 `.avi` 文件，同时导出处理日志（每帧的变换参数、PSNR、SSIM、耗时）为 `.mat` 或 `.csv` 文件
- **实时指标显示：** 每处理完一帧，计算该帧相对于 warp 前的 PSNR（`psnr`）和 SSIM（`ssim`），实时更新在界面上。帧率通过统计最近 10 帧的平均处理时间反算。整体进度条随已处理帧数更新。
- **轨迹可视化子窗口：** 点击「轨迹分析」按钮弹出新窗口（`uifigure`），绘制累积运动轨迹的对比图（原始 vs 平滑后），支持缩放手势。这是答辩演示时的亮点功能。
- **批量对比模式：** 支持一次加载多段测试视频，对每一段分别运行不同参数组合（检测器 × 平滑策略 × 去模糊开关），将结果汇总为对比表格和柱状图，展示在「实验对比」标签页中——这个功能直接服务于 60% 效果分的评估需求。

**性能要求与 MATAB 特有优化：**

- MATLAB App Designer 的单线程特性意味着界面回调函数不能做长时间阻塞运算。处理循环必须放在 `parfeval`（并行后台执行）中，通过 `afterEach` 回调将每帧结果推送回 UI 更新
- 视频帧数据在 MATLAB 中以 `uint8` 矩阵（H×W×3）存储，三阶段流水线的函数签名统一为 `function stabilizedFrame = processFrame(rawFrame, prevFrame, params)`，确保接口一致性
- 使用 `tiledlayout` 在对比模式下将原始帧与处理帧并排显示；使用 `imshowpair` 实现重叠对比模式

**需要了解的 MATLAB 工具箱：** MATLAB App Designer（`uifigure`、`uiaxes`、`uidropdown`、`uislider`、`uiswitch`、`uibutton`）、Parallel Computing Toolbox（`parfeval`、`parfor`）、Image Processing Toolbox（`imshow`、`imresize`、`imcrop`）、Computer Vision Toolbox（`VideoReader`、`VideoWriter`）。


### 角色八：测试与评估工程师（1人）

**核心职责：** 测试素材管理、评估指标体系搭建与自动化计算、消融实验设计执行、结果可视化、性能基准测试。

**具体工作内容：**

**测试素材管理：**

- 对接结课作业统一提供的标准测试视频素材，记录每段视频的元信息：分辨率、帧率、总帧数、场景类型（室内/室外/手持/快速运动/弱光）、抖动类型（高频小幅/低频大幅/旋转/平移为主）、是否含有前景运动物体
- 如果统一素材尚未发放，用公开数据集替代做前期调试：下载 DeepStab 数据集（视频防抖标准基准）中的子集，或用手机自拍 5 段典型场景视频（手持步行、静止站立、快速扫视、上楼梯、骑行颠簸）作为开发阶段的内部测试集
- 管理所有测试素材的版本和路径，在 MATLAB 项目中建立统一的 `data/` 目录结构，`VideoReader` 通过相对路径读取

**评估指标实现：**

- 帧间 PSNR（峰值信噪比）：对连续两帧计算 `psnr(frame_{t}, frame_{t-1})`，取全序列均值。原始视频的帧间 PSNR 低（因为抖动导致帧间差大），稳定后视频的帧间 PSNR 应显著提升
- 帧间 SSIM（结构相似性）：`ssim(frame_{t}, frame_{t-1})`，取全序列均值。与 PSNR 互补——PSNR 对亮度敏感、SSIM 对结构变化敏感
- 稳定比（Stabilization Ratio, SR）：定义为稳定后帧间运动量的均值 / 原始帧间运动量的均值。SR < 1 表示有稳定效果，SR 越小效果越好。帧间运动量用模块一输出的全局变换矩阵的平移分量模长来度量
- 帧率（FPS）：总帧数 / 总处理时间。用 `tic` / `toc` 在每帧处理前后计时，取全序列平均。同时计算首帧延迟（加载模型/初始化检测器的时间）和稳态帧率（跳过前 10 帧后的平均帧率），两者都需在报告中列出
- 各模块耗时拆解：在 MATLAB 代码中为三个模块分别插入计时点（`ticBytes` / `tocBytes` 或手动 `tic` / `toc`），统计运动估计 / 运动分解与平滑 / 帧合成与去模糊各占整个流水线的百分比，绘制饼图

**消融实验设计（这是 60% 效果分的关键）：**

消融实验的目标是回答「系统的每一项设计选择对最终效果的贡献有多大」。实验矩阵设计如下：

| 消融维度 | 变量 | 对照条件 | 评估指标 |
|---------|------|---------|---------|
| 检测器选择 | SIFT 单检测器 vs SIFT+SURF 双检测器协作 | 其他参数不变 | 内点率、匹配成功率、弱纹理场景 SR |
| 光流方案 | 纯 Farneback 光流 vs 稀疏关键点引导光流 | 检测器固定为 SIFT | 运动场精度、动态物体区域误匹配率 |
| 平滑策略（核心消融）| 纯高斯滤波 vs 相对坐标高斯 vs 相对坐标马尔可夫 | 其他参数不变 | 帧间 PSNR、SSIM、SR、长序列累积误差 |
| 去模糊方案 | 不加去模糊 vs 频域维纳 vs 轻量 CNN 去模糊 | 平滑策略固定 | 拉普拉斯方差提升、视觉清晰度 |
| 窗口半径 | l=1 vs l=3 vs l=5 | 平滑策略固定为马尔可夫 | SR、处理帧率（窗口越大计算量越大）|

每组消融实验在同一段测试视频上运行，结果汇总为一张大表，行是消融条件、列是指标。在报告中配以对应的可视化（柱状图对比 + 轨迹折线图对比）。

**失败案例分析：**

不只要找好案例，也要找失败案例。挑出系统处理效果最差的 2-3 个场景帧序列，分析原因：
- 场景特征：高速旋转 / 突然变焦 / 前景占据 70% 以上画面 / 极弱光
- 失效机理：仿射分解误将物体运动当作全局运动？光流在弱纹理区域发散？马尔可夫窗口太短无法捕获低频运镜？
- 改进方向：基于分析结论，在「结论与展望」中写出有根据的局限性和未来工作

**可视化产出（供结课报告使用）：**
- 帧间稳定性对比：选一段典型抖动序列（5 连续帧），2×5 排列，上排原始、下排处理后
- 运动轨迹对比：一条折线图，纵轴为累积位移（像素），横轴为帧号，两条折线分别为原始轨迹和平滑后轨迹
- 各模块耗时饼图
- 消融实验柱状图：多组条件并排、多指标（PSNR/SSIM/SR）分组
- 失败案例标注图：在原始帧上圈出问题区域

**需要精读的论文：** 07_GoPro（评估范式确立）、14_LightStab（消融实验设计参考）、06_盲视频防抖（失败案例分析思路）。

---

## 二、五阶段时间规划

> 总周期：约 11 周。每阶段的具体时间节点需根据课程教学周历微调。以下给出的是阶段时长与任务密度的相对关系——团队人数 8 人，各角色在本阶段的工时占比也一并标注。

### 第一阶段：文献深度研读 + 技术方案定稿 + 环境搭建（第 1-2 周）

| 角色 | 本阶段核心任务 | 产出物 |
|------|---------------|--------|
| 项目负责人 | 通读 17 篇论文总结文档，最终确定各模块的技术路线（哪些做、哪些简化、哪些留「未来工作」）；画出最终版系统架构图（draw.io）；输出三阶段接口规范文档 | 技术方案定稿文档 + 系统架构图 |
| 运动估计 (2人) | 研读论文 14_LightStab 的第 3.1-3.2 节运动估计部分；调研 MATLAB Computer Vision Toolbox 中所有可用的关键点检测器和光流算法，输出功能对比表 | 运动估计技术调研文档 + MATLAB API 功能对比表 |
| 运动分解与平滑 (2人) | 研读论文 17 的相对坐标马尔可夫平滑、论文 09 的仿射分解、论文 15 的 SO(3) 建模；输出平滑策略的数学推导文档（仿射参数 6 自由度 → 增量 → 窗口高斯核的完整公式链） | 平滑策略数学推导文档 |
| 帧合成与去模糊 | 研读论文 12 的门控时空注意力、论文 06 的边界处理策略；调研 MATLAB Image Processing Toolbox 中的去模糊函数（`deconvwnr`、`deconvlucy`、`deconvblind`），输出功能对比表 | 去模糊技术调研文档 |
| App Designer | 学习 MATLAB App Designer 的入门教程；设计第一版界面线框图（手绘或 Figma）；调研 `VideoReader`/`VideoWriter`/`UIAxes` 的实时更新机制 | 界面线框图 + App Designer 学习笔记 |
| 测试评估 | 准备开发阶段自测用的视频素材（公开数据集下载 + 手机自拍）；在 MATLAB 中编写评估指标函数（PSNR、SSIM、SR、帧率统计）的脚手架代码 | 自测素材集 + 评估函数脚手架 `.m` 文件 |

**本阶段结束时召开的全体会议：** 技术方案定稿评审——每个角色汇报调研结论，项目负责人拍板各模块的最终技术选择，输出一份《技术方案定稿》文档（1-2 页），所有人按此方案进入开发阶段。


### 第二阶段：各模块独立开发——最小可行系统（第 3-5 周）

> 这一阶段的目标是跑通三阶段流水线的全流程，但每个模块先用最简化的实现——能跑就行，不求性能。三模块独立开发、每周互相对接口格式。

**第 3 周：搭骨架**

| 角色 | 本阶段核心任务 |
|------|---------------|
| 运动估计 (2人) | 实现 SIFT 单检测器 + RANSAC 仿射拟合的基本流程：`detectSIFTFeatures` → `extractFeatures` → `matchFeatures` → `estimateGeometricTransform2D`。输入两帧，输出 3×3 仿射矩阵。在自拍视频上测试 |
| 运动分解与平滑 (2人) | 实现 6 维仿射参数提取函数（从 3×3 矩阵中解析平移/旋转/缩放/剪切分量），以及最简单的高斯平滑函数（`smoothdata`）。从模块一拿到仿射矩阵序列，输出平滑后的参数序列 |
| 帧合成与去模糊 | 实现仿射 warp + 反射填充 + 裁剪的基本流程：`affine2d` → `imwarp` → `padarray('symmetric')` → `imcrop`。输出稳定帧序列。去模糊先不做 |
| App Designer | 搭建 App Designer 骨架界面：左右两个 `UIAxes`、视频加载按钮、开始处理按钮、进度条。实现 `VideoReader` 读取 + 逐帧显示在左侧轴。右侧轴暂用占位图 |
| 测试评估 | 在自拍视频上运行 MVP 系统，记录第一版全流程的帧间 PSNR 和 SR 作为 baseline |

**第 4-5 周：升级组件**

| 角色 | 本阶段核心任务 |
|------|---------------|
| 运动估计 (2人) | 加 SURF 双检测器协作（第 4 周）；实现稀疏关键点引导的光流融合（`opticalFlowFarneback` + 关键点邻域保留 + 邻域外插值）（第 5 周） |
| 运动分解与平滑 (2人) | 用仿真数据验证仿射分解的正确性（第 4 周）；实现相对坐标建模 + 马尔可夫窗口平滑（第 5 周） |
| 帧合成与去模糊 | 从运动参数中估计模糊核 + 实现频域维纳去卷积（`fft2` → `deconvwnr` → `ifft2`）（第 4-5 周）；如果频域方案效果不佳，在第 5 周尝试搭建轻量 CNN 去模糊网络 |
| App Designer | 加入参数控制面板（`UIDropDown` + `UISpinner`），实现参数回调函数（第 4 周）；实现处理后帧实时更新到右侧 `UIAxes` + 进度条联动（第 5 周） |
| 测试评估 | 在各模块升级后重新跑定量指标，与 MVP 版本对比；为每个升级组件撰写一段「是否有提升」的分析小结 |

**本阶段关键的跨角色对接节点：**

- 第 3 周末：模块一 → 模块二，第一次数据交接（仿射矩阵序列格式确认）
- 第 4 周末：模块二 → 模块三，第一次数据交接（平滑后参数序列 → warp）
- 第 5 周末：三模块 + App Designer 首次联合运行（可能跑不通，但暴露问题就是本阶段的目标）


### 第三阶段：模块集成 + App Designer 全功能开发（第 6-7 周）

| 角色 | 本阶段核心任务 |
|------|---------------|
| 项目负责人 | 主持模块集成调试：追踪每一次跨模块数据交接中的格式不匹配、维度不一致、数据类型不兼容（MATLAB 的 `double` vs `single` vs `uint8` 是最常见的坑）；制定 Bug 列表并分配修复任务 |
| 运动估计 (2人) | 根据集成测试反馈修复模块一的接口问题；优化匹配阈值（`MatchThreshold`）和 RANSAC 重投影误差阈值，使模块一在多种场景下的匹配内点率 > 60% |
| 运动分解与平滑 (2人) | 根据模块三的 warp 效果反馈，微调马尔可夫平滑的窗口半径 l 和高斯核 σ；确保平滑后参数序列不会导致 warp 后画面过度裁剪（裁剪比例控制在 10% 以内） |
| 帧合成与去模糊 | 修复 warp 后帧边缘的黑边或异常像素；调优维纳去卷积的信噪比参数（`deconvwnr` 的 `NSR` 参数），避免过度锐化带来的振铃伪影 |
| App Designer | **本阶段单人工作量最大：**
  - 实现处理循环的 `parfeval` 异步架构——主处理逻辑跑在后台 workers，通过 `afterEach` 回调更新 UI（第 6 周）
  - 实现「暂停」「恢复」「导出结果」按钮功能（第 6 周）
  - 实现实时指标显示面板：帧率、PSNR、SSIM 的实时更新（第 7 周）
  - 实现轨迹可视化子窗口（第 7 周）
  - 整体 UI 美化和布局微调（第 7 周）
| 测试评估 | 对集成后的完整系统进行首次端到端定量评估（所有指标、所有消融条件），输出第一版实验结果表；标记出效果明显好于预期的场景和明显差于预期的场景 |

**第 7 周末里程碑：** 在 App Designer 中点击「加载视频」→「开始处理」→ 看到右侧实时出现稳定帧 → 点击「导出」得到 `.mp4` 结果文件。这是整个项目第一次端到端可演示的状态。


### 第四阶段：系统测试 + 优化迭代 + 消融实验（第 8-9 周）

| 角色 | 本阶段核心任务 |
|------|---------------|
| 项目负责人 | 根据实验反馈，做出「哪些组件保留、哪些参数调优、哪些简化方案也够了」的最终决策；如果某些模块效果确实不理想，及时定夺替代方案或降级方案，避免卡在某个模块拖累整体进度 |
| 运动估计 (2人) | 针对「测试评估」标记出的弱纹理或失败场景，做针对性调优——可能调整光流方法的金字塔层数、关键点检测器的尺度范围、匹配阈值。在标准测试视频上跑完所有素材 |
| 运动分解与平滑 (2人) | 在所有测试素材上跑完 3 种平滑策略的对比实验（高斯 / 相对坐标高斯 / 相对坐标马尔可夫），将数据填入消融实验表；微调窗口半径 l 和核 σ，找到效果的帕累托最优区 |
| 帧合成与去模糊 | 统计去模糊模块在所有帧上的拉普拉斯方差提升量，出具一份「去模糊模块增益分析」——如果提升量在大多数帧上 < 5%，可以和项目负责人讨论跳过该模块以换取速度（40% 效率分） |
| App Designer | **交付最终版 GUI：** 修复所有 UI 交互 Bug（按钮响应卡顿、进度条不更新、参数切换后未生效）；添加批量对比模式——一次加载多段视频、自动跑多组参数组合、汇总对比图表到「实验对比」标签页；确保导出 `.mp4` 的画质与预览一致 |
| 测试评估 | **本阶段单人工作量最大：**
  - 在所有标准测试素材上跑完整消融实验矩阵（5 个消融维度 × 2-3 个变体 × 多段素材），产出最终版实验结果表
  - 制作所有可视化图表（帧间对比图、轨迹对比图、耗时饼图、消融柱状图）
  - 撰写实验部分的分析文字（作为结课报告第 4 节的初稿）
  - 挑选并标注 2-3 个失败案例，准备分析文字

**第 9 周末里程碑：** 实验结果表填满——不是所有数字都漂亮，但所有格子都有数据。团队对每个数据的「为什么高、为什么低」都能给出基于技术原理的解释。


### 第五阶段：结课报告撰写 + 答辩准备（第 10-11 周）

| 角色 | 本阶段核心任务 |
|------|---------------|
| 项目负责人 | 统稿结课报告全文：统一所有章节的写作口吻、术语一致性、图表编号与引用；撰写摘要 + 引言（第 1 节）+ 结论与展望（第 5 节）；制作答辩 PPT 框架 |
| 运动估计 (2人) | 撰写方法部分中「3.2 运动估计模块」的正式文案（含公式、参数表、设计决策的论文引用）；为报告绘制模块一的示意图 |
| 运动分解与平滑 (2人) | 撰写方法部分中「3.3 运动分解与平滑模块」的正式文案（这是报告中最核心的技术段落，需要详细的数学推导和论文引用）；提供轨迹对比图 |
| 帧合成与去模糊 | 撰写方法部分中「3.4 帧合成与去模糊模块」的正式文案；提供 warp 前后对比图 |
| App Designer | 撰写方法部分中「3.5 在线约束与系统实现」的正式文案（含系统架构图、各模块耗时表）；确保 GUI 可运行，准备答辩时的现场演示 |
| 测试评估 | 撰写实验部分「第 4 节」的完整初稿：实验设置 + 定量结果表 + 消融分析 + 定性可视化 + 失败案例分析。撰写相关工作「第 2 节」的初稿（三层叙事框架，参考 00_写作向导 和 00_研究脉络 的结构） |

**第 11 周：**
- 项目负责人统稿终审，确保参考文献格式统一（GB/T 7714），去除 AI 味的表述（参考 00_写作向导 中的写作原则部分）
- 全体进行 2 次模拟答辩，每次 15 分钟 PPT 讲解 + 10 分钟 App Designer 现场演示
- 根据模拟答辩反馈修改 PPT 的报告叙事逻辑，确保「为什么这么做」比「我们做了什么」占比更大——老师更想看决策过程
- 最终提交：GitHub 代码仓库（含 README 环境配置说明）+ 结课报告 PDF + App Designer 可运行工程 `.mlapp` + 处理结果示例视频


## 三、技术方案总结：一页纸速览

```
                    输入: 不稳定视频帧序列
                              │
         ┌────────────────────┼────────────────────┐
         ▼                                           ▼
┌─────────────────────┐               ┌─────────────────────┐
│  模块一: 运动估计    │               │ 关键论文:            │
│  SIFT+SURF 双检测器  │  ──────────▶  │ 14_LightStab CVPR26  │
│  + RANSAC 仿射拟合   │   仿射矩阵     │ 10_光流防抖 CVPR20   │
│  + 因果光流融合      │               │ 11_多网格 TIP19     │
└─────────────────────┘               └─────────────────────┘
         │
         │ 仿射矩阵序列
         ▼
┌─────────────────────┐               ┌─────────────────────┐
│ 模块二: 运动分解与平滑│               │ 关键论文:            │
│  6参数仿射分解       │  ──────────▶  │ 17_在线拼接防抖 MDPI25│
│  → 相对坐标建模      │   平滑参数     │ 09_GlobalFlowNet    │
│  → 马尔可夫窗口平滑   │               │ 15_微跳视 SO(3)     │
└─────────────────────┘               └─────────────────────┘
         │
         │ 平滑后变换参数
         ▼
┌─────────────────────┐               ┌─────────────────────┐
│ 模块三: 帧合成与去模糊│               │ 关键论文:            │
│  仿射 warp + 边界填充 │  ──────────▶  │ 06_盲视频防抖        │
│  + 频域维纳去卷积     │   稳定帧输出   │ 12_门控注意力 CVPR21 │
│  + 轻量去模糊后处理   │               │ 16_CompEvent 频域   │
└─────────────────────┘               └─────────────────────┘
         │
         ▼
┌─────────────────────┐
│ MATLAB App Designer │
│  实时预览 + 参数调节  │
│  + 指标显示 + 导出   │
└─────────────────────┘
```

**一句话技术路线：** 用 LightStab（14）的模块化架构搭框架，用仿射分解 + 相对坐标马尔可夫平滑（09+17）做核心的运动区分与平滑，用频域维纳去卷积（16 启发）做轻量去模糊后处理，整体部署在 MATLAB App Designer 的异步多线程 GUI 中——在效果（60%）和效率（40%）之间取得最优平衡。


## 四、17 篇论文在各模块中的引用规划

| 论文编号 | 论文简称 | 引用位置 | 引用目的 |
|---------|---------|---------|---------|
| 01 | 综述 arXiv2401 | Related Work / 结论 | 定位本方法在去模糊演进中的位置、CNN vs Transformer 效率对比 |
| 02 | 元学习防抖 | Related Work / 结论 | 测试时自适应的「未来工作」方向 |
| 03 | SPEINet | Related Work | 清晰度先验的网络设计思路（去模糊模块的可选借鉴） |
| 04 | VJT Transformer | Related Work | Transformer 联合任务的范式提及 |
| 05 | DSFN 联合增强 | Related Work | 多任务共享特征的效率优势 |
| 06 | 深度运动盲防抖 | 方法 3.4 / 实验 | 全帧输出策略（warp 后边界处理）、盲估计方法对比 baseline |
| 07 | GoPro 手持去模糊 | 引言 / Related Work / 实验 | 问题定义来源、GoPro 数据集评估范式、模糊核物理建模 |
| 08 | 联合插值去模糊 | Related Work / 结论 | 多任务互利的理论依据、「未来工作」中插帧+去模糊方向 |
| 09 | GlobalFlowNet | 方法 3.3 / 结论 | 仿射分解思路来源、知识蒸馏的「未来工作」方向 |
| 10 | 光流防抖 CVPR20 | 方法 3.2 / Related Work | 光流用于防抖的范式确立、运动估计参考 |
| 11 | 多网格 warping TIP | 方法 3.2 / 效率分析 | 多网格 warp 替代全局仿射的可选方案、在线处理范式 |
| 12 | 门控时空注意力 CVPR21 | 方法 3.4 / Related Work | 去模糊子模块的核心参考、选择性计算策略 |
| 13 | 无监督拼接防抖 MDPI25 | Related Work / 结论 | 无监督损失函数设计参考 |
| 14 | **LightStab CVPR26** | **方法全章 / 引言** | **整体架构参考、多检测器协作、因果光流融合、多线程异步——最重要的引用** |
| 15 | 微跳视补偿 SO(3) | 方法 3.3 / 结论 | SO(3) 物理建模的「未来工作」方向、递归优化热启动策略 |
| 16 | CompEvent 事件融合 | 方法 3.4 / 结论 | 频域去模糊思路来源、事件相机的「未来工作」方向 |
| 17 | **在线拼接防抖 MDPI25** | **方法 3.3 / 实验** | **相对坐标马尔可夫平滑核心参考、动态注意力掩码——第二重要的引用** |


## 五、关键风险与应对预案

| 风险 | 影响模块 | 概率 | 应对预案 |
|------|---------|------|---------|
| MATLAB 光流算法在弱纹理场景效果差 | 模块一 | 中 | 退化为纯关键点匹配（不做光流融合），关键点匹配失败时使用前一帧的变换矩阵作为当前帧的近似（零阶保持） |
| 相对坐标马尔可夫平滑的代码实现比预期复杂 | 模块二 | 中 | 降级为绝对坐标高斯滤波（代码量小、一定能跑通）。在报告中讨论降级原因，将相对坐标方案写进「未来工作」 |
| 频域维纳去卷积引入振铃伪影 | 模块三 | 中高 | 跳过去模糊模块（报告中说清楚 warp 后残留模糊的程度，并指出这是频域盲去卷积的已知局限） |
| App Designer + parfeval 异步架构调试困难 | GUI | 高 | 降级为同步处理（界面在处理期间卡住但功能正确）。先确保功能对再追求体验好，答辩时可以做「非实时但有完整功能」的演示 |
| 标准测试素材发放时间晚 | 测试 | 中 | 先用自拍素材 + DeepStab 公开数据集完成全部开发和调试。标准素材拿到后只需换入 `VideoReader` 路径重新跑一遍实验 |
| 某个模块进度严重落后 | 整体 | 中 | 模块接口解耦的好处：任一模块的降级或简化不影响其他模块的开发。项目负责人需在每周末评估各模块实际进度 vs 计划，发现偏差 > 2 天立刻介入调整范围 |
