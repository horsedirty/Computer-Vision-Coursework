# 角色六初稿兼容性问题

> 记录：2026-05-25  
> 涉及文件：`estimate_blur_kernel.m`、`wiener_deblur.m`

---

## 问题：`fspecial` 在 MATLAB R2024a+ 中被标记为 not recommended

### 现状

当前代码在两处使用了 `fspecial`：

| 文件 | 行位置 | 调用 | 用途 |
|------|--------|------|------|
| `estimate_blur_kernel.m` | ~83 | `fspecial('motion', psfLen, psfAngle)` | 生成线状运动模糊核 |
| `wiener_deblur.m` | ~184 | `fspecial('laplacian', 0.2)` | 生成拉普拉斯算子（清晰度评估） |

### 风险等级

**低风险**。理由：

1. 本项目目标 MATLAB 版本为 R2022b+，`fspecial` 在 R2022b 中为完全推荐的函数
2. MATLAB 目前只是"建议替代"而非"计划移除"，短期内不会报错或删除
3. `fspecial('motion', ...)` 和 `fspecial('laplacian', ...)` 在官方迁移指南中**没有直接替代函数**，即使升级到最新版 MATLAB 也能正常使用

### 何时需要修改

- 如果 MATLAB 官方将 `fspecial` 标记为 `removed`（目前无此计划）
- 如果其他团队成员使用最新的 MATLAB R2025+ 运行代码且出现 warning

### 修改方案（备用）

如需完全消除 `fspecial` 依赖，改为手动构建等价矩阵：

#### 方案 A：替换 `fspecial('motion', len, angle)`（运动模糊核）

```matlab
function psf = motion_kernel(len, angle_deg)
    % 手动构建线状运动模糊核，替代 fspecial('motion', len, angle)
    % angle_deg: 模糊方向（度），len: 模糊长度（像素，奇数）
    if len < 2
        psf = 1;
        return;
    end
    len = max(ceil(len), 1);
    if mod(len, 2) == 0
        len = len + 1;  % 确保奇数
    end
    half = (len - 1) / 2;
    [x, y] = meshgrid(-half:half, -half:half);

    % 旋转到指定角度
    angle_rad = deg2rad(angle_deg);
    % 计算每个像素到中心线的距离
    dist = abs(-x * sin(angle_rad) + y * cos(angle_rad));
    % 沿中心线方向的范围
    along = x * cos(angle_rad) + y * sin(angle_rad);

    % 在一条长度约为 len、宽度约为 1 像素的线段上赋值为 1
    psf = double(dist <= 0.5 & abs(along) <= half);
    psf = psf / sum(psf(:));
end
```

#### 方案 B：替换 `fspecial('laplacian', alpha)`（拉普拉斯算子）

```matlab
function kernel = laplacian_kernel(alpha)
    % 手动构建拉普拉斯算子，替代 fspecial('laplacian', alpha)
    % alpha: 形状参数，默认 0.2
    if alpha == 0
        kernel = [0,  1, 0;
                  1, -4, 1;
                  0,  1, 0];
    else
        kernel = alpha / (1 + alpha) * ...
                 [1 - alpha, alpha,     1 - alpha;
                  alpha,     -4/alpha,  alpha;
                  1 - alpha, alpha,     1 - alpha];
    end
end
```

### 涉及的具体修改位置

**`estimate_blur_kernel.m` 第 83 行：**
```matlab
% 旧：
psf = fspecial('motion', psfLen, psfAngle);

% 新：
psf = motion_kernel(psfLen, psfAngle);
```

**`wiener_deblur.m` 第 184 行：**
```matlab
% 旧：
laplacianKernel = fspecial('laplacian', 0.2);

% 新：
laplacianKernel = laplacian_kernel(0.2);
```

---

## 其他检查项（均已通过，无问题）

| 检查项 | 状态 |
|--------|------|
| 无 Python / C++ MEX 调用 | ✅ |
| 无硬编码文件路径 | ✅ |
| 全部使用 `arguments` 块 | ✅ |
| 未修改其他模块签名 | ✅ |
| 未使用 `removed` 函数 | ✅ |
| 符合数据契约 (`3×3` 矩阵、`uint8` 帧) | ✅ |
| `diagnostics` 含 `_time_ms` 字段 | ✅ |
| 每文件含自测块 | ✅ |
| 函数体 < 80 行（主函数） | ✅ |
