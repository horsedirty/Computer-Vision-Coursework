# 角色二：关键点检测与匹配模块说明书

## 概述

本文档整合了角色二（关键点检测与匹配方向）的全部代码文件，说明各函数的调用关系、输入输出格式、核心算法原理和参数调优指南。

角色二负责三阶段流水线中**模块一（运动估计）** 的关键点检测与匹配子任务，输出匹配点对集合供后续光流融合和 RANSAC 全局变换估计使用。

---

## 文件清单与调用关系

```
main_pipeline.m  (主流水线, 全角色共用)
  └─ run_motion_estimation.m  (模块一主入口, 角色二&三共享)
       ├─ detect_keypoints.m      (角色二: 多检测器协作关键点检测)
       ├─ match_keypoints.m       (角色二: 关键点特征匹配)
       ├─ classify_scene.m        (角色二: 场景类型分类)
       ├─ compute_optical_flow.m  (角色三, TODO)
       ├─ fuse_motion_field.m     (角色三, TODO)
       └─ estimate_global_transform.m  (角色二&三协作, TODO)
```

- `detect_keypoints.m` → 检测并输出关键点坐标 + 置信度 + 来源检测器标签
- `match_keypoints.m` → 对连续两帧的关键点做特征匹配，输出匹配点对
- `classify_scene.m` → 根据光流和关键点密度判断当前场景类型
- `run_motion_estimation.m` → 串联上述步骤，输出全局变换矩阵 + 完整诊断信息
- `main_pipeline.m` → 全视频循环调用 + 按场景聚合各检测器内点率

---

## 一、detect_keypoints.m —— 多检测器协作关键点检测

### 函数签名

```matlab
function [keypoints, scores, detectorMap, diagnostics] = detect_keypoints(frame, params)
```

### 输入参数

| 参数 | 类型 | 说明 |
|------|------|------|
| `frame` | `uint8 H×W×3` 或 `H×W` | 输入帧图像, 彩色或灰度 |
| `params` | `struct` | 参数字典, 支持以下字段 |

**params 字段说明：**

| 字段 | 默认值 | 说明 |
|------|--------|------|
| `detectors` | `{'SIFT', 'SURF'}` | 检测器列表, 可选 `'SIFT'`/`'SURF'`/`'ORB'` 任意组合 |
| `sift_nPoints` | 500 | SIFT 保留点数 |
| `surf_nPoints` | 500 | SURF 保留点数 |
| `orb_nPoints` | 500 | ORB 保留点数 |
| `harris_nPoints` | 100 | Harris 保底追加点数 |
| `nms_radius` | 8 | NMS 抑制半径（像素） |
| `grid_rows` | 6 | 网格均匀化行数 |
| `grid_cols` | 8 | 网格均匀化列数 |
| `top_k_per_grid` | 5 | 每格保留 Top-K 个最高分点 |
| `min_dist` | 5 | 相邻点最小间距（像素） |
| `min_keypoints` | 30 | 弱纹理判定阈值, 低于此值自动追加 Harris 角点 |

### 输出参数

| 输出 | 类型 | 说明 |
|------|------|------|
| `keypoints` | `M×2 double` | 合并后的关键点坐标 `[x, y]` |
| `scores` | `M×1 double` | 对应置信度分数（降序排列） |
| `detectorMap` | `M×1 cell` | 每点来源检测器, 如 `{'SIFT'; 'SURF'; ...}` |
| `diagnostics` | `struct` | 诊断信息 |

**diagnostics 结构体：**

| 字段 | 说明 |
|------|------|
| `raw_counts.SIFT_raw` | SIFT 原始检测数 |
| `raw_counts.SURF_raw` | SURF 原始检测数 |
| `raw_counts.ORB_raw` | ORB 原始检测数 |
| `raw_counts.nms_removed` | NMS 剔除点数 |
| `raw_counts.harris_added` | Harris 追加点数 |
| `final_counts.SIFT_final` | SIFT 最终保留数 |
| `final_counts.SURF_final` | SURF 最终保留数 |
| `final_counts.HARRIS_final` | Harris 最终保留数 |
| `final_counts.total` | 最终总点数 |
| `detect_time_ms` | 检测耗时（毫秒） |

### 处理流程

```
输入帧 → 灰度转换
  → 对每个检测器分别调用 (SIFT_detect / SURF_detect / ORB_detect)
    → 合并为 allPoints + allScores + allDetectors
  → 按置信度降序排序
  → NMS 非极大值抑制 (高分点抑制邻域内低分点, radius=8px)
  → [弱纹理保底] 总点数 < min_keypoints?
      → 是: 追加 Harris 角点 (向量化欧氏距离去重)
  → 网格均匀化 SSC (6×8 网格, 每格 Top-5, 5px 最小间距)
  → 输出 keypoints + scores + detectorMap + diagnostics
```

### 核心子函数

| 子函数 | 说明 |
|--------|------|
| `SIFT_detect(grayFrame, nPoints)` | `detectSIFTFeatures` + `selectStrongest` |
| `SURF_detect(grayFrame, nPoints)` | `detectSURFFeatures` + `selectStrongest` |
| `ORB_detect(grayFrame, nPoints)` | `detectORBFeatures` + `selectStrongest` |
| `HARRIS_detect(grayFrame, nPoints)` | `detectHarrisFeatures` + `selectStrongest` |
| `nms_merge(points, scores, detectors, radius)` | 降序排序后逐点抑制邻域内低分点 |
| `spatial_selective_clustering(...)` | 网格划分 → 每格 Top-K → 最小间距约束 |

### 调参建议

| 场景 | 建议参数 |
|------|---------|
| 弱纹理视频 | `detectors={'SIFT'}` 或 `{'SIFT','SURF'}`, `min_keypoints=50`, `harris_nPoints=200` |
| 快速运动 | `detectors={'SIFT','SURF'}`, `nms_radius=6`, `top_k_per_grid=8` |
| 高分辨率视频 | `sift_nPoints=1000`, `surf_nPoints=1000`, `grid_rows=8`, `grid_cols=12` |
| 追求速度 | `detectors={'SURF'}`, `surf_nPoints=300`, `grid_rows=4`, `grid_cols=6` |

### 调用示例

```matlab
params.detectors = {'SIFT', 'SURF', 'ORB'};
params.sift_nPoints = 800;
params.surf_nPoints = 600;
params.nms_radius = 10;
params.top_k_per_grid = 6;

[kp, scores, detMap, diag] = detect_keypoints(frame, params);
fprintf('检测到 %d 个关键点, 耗时 %.1f ms\n', size(kp,1), diag.detect_time_ms);
```

---

## 二、match_keypoints.m —— 关键点特征匹配

### 函数签名

```matlab
function [matchedPrev, matchedCurr, matchScores, stats] = ...
    match_keypoints(prevFrame, currFrame, prevKP, currKP, params)
```

### 输入参数

| 参数 | 类型 | 说明 |
|------|------|------|
| `prevFrame` | `uint8 H×W×3` 或 `H×W` | 前一帧图像 |
| `currFrame` | `uint8 H×W×3` 或 `H×W` | 当前帧图像 |
| `prevKP` | `M×2 double` | 前一帧关键点坐标 |
| `currKP` | `N×2 double` | 当前帧关键点坐标 |
| `params` | `struct` | 参数字典 |

**params 字段说明：**

| 字段 | 默认值 | 说明 |
|------|--------|------|
| `descriptor` | `'SIFT'` | 描述子类型, `'SIFT'`/`'SURF'`/`'FREAK'` |
| `maxRatio` | 0.6 | Lowe's ratio test 阈值, 越小匹配越严格 |
| `unique` | `true` | 是否保留唯一匹配（禁止一对多） |
| `matchMethod` | `'Exhaustive'` | `'Exhaustive'` 穷举 / `'Approximate'` 近似 |
| `matchThreshold` | 10 | 匹配阈值（百分比） |

### 输出参数

| 输出 | 类型 | 说明 |
|------|------|------|
| `matchedPrev` | `K×2 double` | 匹配上的前一帧点坐标 |
| `matchedCurr` | `K×2 double` | 匹配上的当前帧点坐标 |
| `matchScores` | `K×1 double` | 匹配距离（越小越好） |
| `stats` | `struct` | 匹配统计信息 |

**stats 结构体：**

| 字段 | 说明 |
|------|------|
| `numMatches` | 成功匹配的点对数 K |
| `numPrevFeatures` | 前一帧有效特征数 |
| `numCurrFeatures` | 当前帧有效特征数 |
| `meanDistance` | 平均匹配距离 |
| `ratioTestRejected` | Lowe's ratio 拒绝的匹配数 |
| `match_time_ms` | 匹配耗时（毫秒） |

### 处理流程

```
输入前一帧 + 当前帧 + 关键点坐标
  → 灰度转换
  → cornerPoints(prevKP) + cornerPoints(currKP)  (裸坐标转关键点对象)
  → extractFeatures 提取描述子 (SIFT/SURF/FREAK)
  → matchFeatures(..., 'MaxRatio', 0.6)  (Lowe's ratio test)
    → 输出匹配点对索引 + 匹配距离
  → 空安全处理: 空帧/无特征/无匹配 三种情况均返回空结果
  → 输出 matchedPrev + matchedCurr + matchScores + stats
```

### 调用示例

```matlab
params.descriptor = 'SIFT';
params.maxRatio = 0.6;
params.unique = true;

[mPrev, mCurr, scores, stats] = match_keypoints(prevFrame, currFrame, prevKP, currKP, params);
fprintf('匹配 %d 对, 平均距离 %.2f, 拒绝 %d 对\n', ...
    stats.numMatches, stats.meanDistance, stats.ratioTestRejected);
```

### 注意事项

- `matchFeatures` 的 `MaxRatio` 参数实现了 Lowe's ratio test：计算最近邻距离 / 次近邻距离，如果比值大于 `maxRatio` 则拒绝该匹配，有效剔除模糊匹配
- 至少需要 4 对匹配点才能给下游 `estimate_global_transform` 做 RANSAC 仿射/投影变换估计
- `'SURF'` 描述子在弱纹理场景比 `'SIFT'` 更鲁棒，但匹配速度略慢

---

## 三、classify_scene.m —— 场景类型分类

### 函数签名

```matlab
function sceneType = classify_scene(flow, keypointCount, params)
```

### 输入参数

| 参数 | 类型 | 说明 |
|------|------|------|
| `flow` | `opticalFlow` 对象 | 含 `Vx`/`Vy`/`Magnitude` 属性的光流对象 |
| `keypointCount` | `double` | 前一帧检测到的关键点总数 |
| `params` | `struct` | 参数字典 |

**params 字段说明：**

| 字段 | 默认值 | 说明 |
|------|--------|------|
| `minKeypoints` | 30 | 弱纹理判定阈值 |
| `flowMeanStatic` | 0.5 | 静态场景光流均值上限 |
| `flowMeanLarge` | 3.0 | 大运动场景光流均值下限 |
| `flowStdRatio` | 2.0 | 动态前景判定: 局部块均值 / 全局均值 比值下限 |

### 输出参数

| 输出 | 类型 | 说明 |
|------|------|------|
| `sceneType` | `char` | `'static'`/`'dynamic_foreground'`/`'low_texture'`/`'large_motion'` |

### 判定优先级

```
关键点 < minKeypoints → low_texture
光流均值 > flowMeanLarge → large_motion
光流均值 > flowMeanStatic 且 局部4×4块中某块均值/全局均值 > flowStdRatio → dynamic_foreground
否则 → static
```

### 场景类型说明

| 类型 | 含义 | 典型场景 |
|------|------|---------|
| `static` | 静态背景 | 三脚架拍摄、固定摄像头 |
| `dynamic_foreground` | 动态前景 | 行人走过、车辆驶过、风吹树叶 |
| `low_texture` | 弱纹理 | 白墙、天空、光滑地面 |
| `large_motion` | 大运动 | 快速扫视、剧烈抖动、骑行颠簸 |

### 调用示例

```matlab
flow = opticalFlowLKDoG(prevGray, currGray);
sceneType = classify_scene(flow, size(keypoints, 1));
fprintf('当前场景类型: %s\n', sceneType);
```

---

## 四、run_motion_estimation.m —— 模块一主入口

### 函数签名

```matlab
function [T, diagnostics] = run_motion_estimation(prevFrame, currFrame, params)
```

### 输入参数

| 参数 | 类型 | 说明 |
|------|------|------|
| `prevFrame` | `uint8 H×W×3` | 前一帧彩色图像 |
| `currFrame` | `uint8 H×W×3` | 当前帧彩色图像 |
| `params` | `struct` | 全局参数字典 |

**params 子模块参数：**

| 字段 | 默认值 | 子模块 |
|------|--------|--------|
| `params.detect_params` | `struct()` | → 传给 `detect_keypoints` |
| `params.match_params` | `struct()` | → 传给 `match_keypoints` |
| `params.flow_params` | `struct()` | → 传给 `compute_optical_flow`（角色三） |
| `params.fuse_params` | `struct()` | → 传给 `fuse_motion_field`（角色三） |
| `params.transform_params` | `struct()` | → 传给 `estimate_global_transform`（角色二&三） |
| `params.use_sparse_fusion` | `true` | 是否启用稀疏光流融合 |

### 输出参数

| 输出 | 类型 | 说明 |
|------|------|------|
| `T` | `3×3 double` | 全局变换矩阵（当前帧相对于前一帧） |
| `diagnostics` | `struct` | 完整诊断信息 |

**diagnostics 结构体：**

| 字段 | 说明 |
|------|------|
| `detect_time_ms` | 关键点检测耗时 |
| `match_time_ms` | 关键点匹配耗时 |
| `flow_time_ms` | 光流计算耗时 |
| `fuse_time_ms` | 光流融合耗时 |
| `transform_time_ms` | RANSAC 变换估计耗时 |
| `total_time_ms` | 模块总耗时 |
| `keypoint_count` | `[prevKP, currKP]` 两帧关键点数量 |
| `match_count` | 成功匹配点数 |
| `inlier_ratio` | RANSAC 全局内点率 |
| `scene_type` | 场景类型: `'static'`/`'dynamic_foreground'`/`'low_texture'`/`'large_motion'` |
| `per_detector_stats` | 各检测器内点率统计 |
| `detect_details` | `{detectDiag1, detectDiag2}` 两帧检测详情 |
| `match_details` | 匹配详情 |
| `flow_details` | 光流详情 |
| `fuse_details` | 融合详情 |
| `transform_details` | 变换估计详情 |

**per_detector_stats 结构体示例：**

```matlab
per_detector_stats.SIFT  = struct('total', 120, 'inlier', 102, 'inlierRate', 0.85)
per_detector_stats.SURF  = struct('total', 80,  'inlier', 56,  'inlierRate', 0.70)
per_detector_stats.HARRIS = struct('total', 15, 'inlier', 12,  'inlierRate', 0.80)
```

### 六步处理流程

```
步骤 1: detect_keypoints(prevFrame) + detect_keypoints(currFrame)
        → [prevKP, prevScores, prevDetMap] + [currKP, ...]

步骤 2: match_keypoints(prevFrame, currFrame, prevKP, currKP)
        → matchedPrev + matchedCurr

步骤 3: compute_optical_flow(prevGray, currGray)   [角色三]
        → flow + flowDiag

步骤 4: fuse_motion_field(flow, matchedPrev, matchedCurr)   [角色三]
        → fusedFlow + fuseDiag

步骤 5: estimate_global_transform(matchedPrev, matchedCurr)   [角色二&三]
        → T + inlierIdx + transformDiag

步骤 6a: classify_scene(flow, size(prevKP,1))
         → sceneType

步骤 6b: ismember(matchedPrev, prevKP) 回溯 detectorMap
         → per_detector_stats (各检测器内点率)
```

### per_detector_stats 计算原理

```matlab
[~, idxInPrev] = ismember(matchedPrev, prevKP, 'rows');   % 匹配点回溯
matchedDetectors = prevDetMap(idxInPrev);                   % 取来源检测器
for each detector:
    detMask = strcmp(matchedDetectors, detName);
    nTotal = sum(detMask);       % 该检测器贡献的匹配数
    nInlier = sum(inlierIdx(detMask));  % 其中内点数
    inlierRate = nInlier / nTotal;
end
```

---

## 五、main_pipeline.m —— 主流水线（角色二新增部分）

### 新增代码位置

在原有三阶段流水线中，**运动估计循环之后、运动平滑之前**，插入了"第二步插曲"——各检测器内点率 × 场景类型聚合统计。

### 聚合逻辑

```matlab
for i = 2:N                                    % 遍历所有帧
    sceneType = diag_motion{i}.scene_type;      % 取场景类型
    stats = diag_motion{i}.per_detector_stats;  % 取检测器内点率
    sceneAgg.(sceneType).(detName).totalTotal  += stats.(detName).total;
    sceneAgg.(sceneType).(detName).inlierTotal += stats.(detName).inlier;
end
% 最后计算: inlierRate = inlierTotal / totalTotal
```

### 控制台输出示例

```
[Pipeline] 各场景检测器内点率聚合:
  [static] 450帧
    SIFT   total: 54000, inlier: 48600, rate: 0.90
    SURF   total: 36000, inlier: 28800, rate: 0.80
  [low_texture] 80帧
    SIFT   total: 2400,  inlier: 960,   rate: 0.40
    HARRIS total: 4800,  inlier: 4080,  rate: 0.85
  [large_motion] 70帧
    SIFT   total: 8400,  inlier: 7560,  rate: 0.90
    SURF   total: 5600,  inlier: 3920,  rate: 0.70
```

### 诊断保存

聚合结果 `sceneAgg` 随其他诊断数据保存到 `outputVideo_diag.mat`，可在 MATLAB 中加载查看：

```matlab
load('data/results/stabilized_diag.mat', 'sceneAgg')
sceneAgg
```

---

## 六、数据流总图

```
                                         ┌──────────────────┐
                                         │   detect_keypoints│
                                         │  ┌─────┐┌─────┐  │
                         prevFrame ──────▶  │SIFT ││SURF │  │
                                         │  └──┬──┘└──┬──┘  │
                                         │     └──┬───┘     │
                                         │     NMS 合并     │
                                         │  Harris 保底     │
                                         │  SSC 网格均匀化   │
                                         │     │    │       │
                                         │  keypoints detectorMap│
                                         └─────┼────┼──────────┘
                                               │    │
                     currFrame ───────┐         │    │
                                     │         │    │
                                     ▼         ▼    ▼
                              ┌──────────────────────────┐
                              │    match_keypoints        │
                              │  extractFeatures          │
                              │  matchFeatures (MaxRatio) │
                              │         │                 │
                              │   matchedPrev + matchedCurr│
                              └─────────┼─────────────────┘
                                        │
                                        ▼
                         ┌──────────────────────────────┐
                         │  estimate_global_transform    │
                         │  (RANSAC, 角色二&三协作)      │
                         │         │                     │
                         │   T (3×3) + inlierIdx         │
                         └─────────┼─────────────────────┘
                                   │
                                   ▼
                   ┌──────────────────────────────┐
                   │  classify_scene              │
                   │  flow + keypointCount        │
                   │  → sceneType                 │
                   └─────────┬────────────────────┘
                             │
                             ▼
                   ┌──────────────────────────────┐
                   │  内点率回溯                   │
                   │  matchedPrev → ismember →    │
                   │  prevDetMap → 分组统计        │
                   │  → per_detector_stats         │
                   └─────────┬────────────────────┘
                             │
                             ▼
                   ┌──────────────────────────────┐
                   │  diagnostics 输出:            │
                   │  scene_type                  │
                   │  per_detector_stats           │
                   │  inlier_ratio                │
                   └──────────────────────────────┘
```

---

## 七、场景聚合结果解读指南

### 自适应检测器切换策略（基于聚合结果）

对 `sceneAgg` 的分析可以指导后续的自适应切换策略：

| 场景 | 高内点率检测器 | 建议策略 |
|------|---------------|---------|
| `static` | SIFT / SURF 均高 | 保持默认 SIFT+SURF |
| `low_texture` | HARRIS 远高于 SIFT | 自动切换到 Harris 为主 |
| `dynamic_foreground` | SIFT 优于 SURF | 仅启用 SIFT + 降低 NMS radius |
| `large_motion` | SURF 速度快于 SIFT | 优先 SURF 以提升帧率 |

### 示例操作

```matlab
load('results_diag.mat', 'sceneAgg');
sceneTypes = fieldnames(sceneAgg);
for s = 1:length(sceneTypes)
    st = sceneTypes{s};
    fprintf('[%s] %d帧\n', st, sceneAgg.(st).frameCount);
    dets = setdiff(fieldnames(sceneAgg.(st)), {'frameCount'});
    [~, bestIdx] = max([sceneAgg.(st).(dets{1}).inlierRate, ...
                         sceneAgg.(st).(dets{2}).inlierRate]);
    fprintf('  推荐检测器: %s (内点率 %.2f)\n', ...
        dets{bestIdx}, sceneAgg.(st).(dets{bestIdx}).inlierRate);
end
```

---

## 八、快速开始

### 单帧测试

```matlab
% 读取两帧
v = VideoReader('test_video.mp4');
prev = read(v, 1);
curr = read(v, 2);

% 检测关键点
detect_params.detectors = {'SIFT', 'SURF'};
[kp_prev, ~, detMap, diag_d] = detect_keypoints(prev, detect_params);

% 匹配
match_params.maxRatio = 0.6;
[mPrev, mCurr, ~, stats] = match_keypoints(prev, curr, kp_prev, ...
    detect_keypoints(curr, detect_params), match_params);

% 场景分类
flow = opticalFlowFarneback(rgb2gray(prev), rgb2gray(curr));
sceneType = classify_scene(flow, size(kp_prev, 1));

% 输出统计
fprintf('关键点: %d, 匹配: %d, 场景: %s\n', ...
    size(kp_prev,1), stats.numMatches, sceneType);
```

### 全视频运行

```matlab
params.inputVideo = 'test_video.mp4';
params.motion_params.detect_params.detectors = {'SIFT', 'SURF'};
params.motion_params.match_params.maxRatio = 0.6;
params.verbose = true;
params.saveDiagnostics = true;

main_pipeline(params);
% 查看结果:
%   1. data/results/stabilized.mp4 — 稳定视频
%   2. data/results/stabilized_diag.mat — 完整诊断 + sceneAgg
```

---

## 九、依赖工具箱

| 工具箱 | 用途 |
|--------|------|
| Computer Vision Toolbox | `detectSIFTFeatures`, `detectSURFFeatures`, `detectORBFeatures`, `detectHarrisFeatures`, `extractFeatures`, `matchFeatures`, `opticalFlow` 等 |
| Image Processing Toolbox | `rgb2gray` |

**无其他依赖。** 所有函数均使用 MATLAB 内置函数，未引入 Statistics Toolbox 或其他第三方库。
